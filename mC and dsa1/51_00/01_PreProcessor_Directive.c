#include <stdio.h>
#define div(x, y) x / y
#define PI 3.14159
#define SQUARE(x) (x*x)


int main()
{

    printf("%d", div(2 + 2, 2)); //it doesnt calculate first but replace
    // it replace like 2+2/2 
}
