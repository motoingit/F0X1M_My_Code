#include "moto_header.h" // Included twice intentionally
#include "moto_header.h" // Included twice intentionally

#include <iostream>
using namespace std;

int main() {
    cout << add(5, 10) << endl;
    cout << divi(20, 5) << endl;
    cout << divi(10, 0) << endl;
    return 0;
}
