class UserMe{
    constructor(
        public email: string, 
        private name: string 
        ){

        this.email = email;
        this.name = name;
    }
}

// this is the way profestion like to write
